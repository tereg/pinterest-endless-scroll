$('#pins').append('<%= j render partial: 'pin', collection: @pins %>');
<% if @pins.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate(@pins) %>');
<% else %>
  $('.pagination').remove();
<% end %>

